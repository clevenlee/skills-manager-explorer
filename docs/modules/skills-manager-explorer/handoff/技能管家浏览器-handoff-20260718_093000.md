# 技能管家浏览器 handoff 2026-07-18 09:30:00（包名 / 模块目录 / 二进制重命名）

## 决策

用户在 1.0.2 计划 Approval Gate 中**批准**把以下三处同步重命名为 `skills-manager-explorer`：

1. `package.json` 的 `name` 字段；
2. `docs/modules/skills-manager-browser/` → `docs/modules/skills-manager-explorer/`，并把目录内 PRD / spec / 1.0.1 plan/todo/verify 中的旧产品名与旧路径引用同步替换；
3. `dist/skills-manager-browser` → `dist/skills-manager-explorer` 单文件产物（以及 `scripts/package.ts` 中的 `--outfile`）。

同时把 OpenAPI 文件名由 `skills-manager-browser-local-openapi.yaml` 改为 `skills-manager-explorer-local-openapi.yaml`，并同步 `package.json` 中 `openapi:lint` / `mock` 脚本路径与 `scripts/generate-openapi.ts` 的 `openApiOutputPath`。

## 实施动作

| 类别                                               | 改动                                                                                                                  |
| -------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| 模块目录                                           | `git mv docs/modules/skills-manager-browser/ docs/modules/skills-manager-explorer/`                                   |
| PRD 文件                                           | `git mv .../prd/skills-manager-browser-prd-1.0.md .../prd/skills-manager-explorer-prd-1.0.md`                         |
| spec 文件                                          | `git mv .../spec/skills-manager-browser-spec-1.0.md .../spec/skills-manager-explorer-spec-1.0.md`                     |
| OpenAPI 文件                                       | `git mv .../openapi/skills-manager-browser-local-openapi.yaml .../openapi/skills-manager-explorer-local-openapi.yaml` |
| `package.json`                                     | `name`、`openapi:lint`、`mock` 三处路径                                                                               |
| `scripts/package.ts`                               | `dist/skills-manager-explorer`                                                                                        |
| `scripts/generate-openapi.ts`                      | `openApiOutputPath` 指向新文件名                                                                                      |
| `tests/integration/database-compatibility.test.ts` | 临时目录前缀 `skills-manager-explorer-`                                                                               |
| `bun.lock`                                         | 删除后 `bun install` 重新生成，`name` 已更新                                                                          |
| 1.0.1 plan / todo / verify                         | 把旧路径与旧产品名批量替换为新名（保留历史记录）                                                                      |
| PRD / spec                                         | 同上                                                                                                                  |

## 验证

- `bun run verify` 通过（format:check、lint、typecheck、test、openapi:check、openapi:lint、build 全绿）。
- `bun run openapi:generate` 在新路径成功生成 `skills-manager-explorer-local-openapi.yaml`，与 Zod 契约一致。
- `rg -n 'skills-manager-browser' src/ tests/ e2e/ scripts/ playwright.config.ts package.json` 无遗留旧名（历史 PRD / spec / 1.0.1 plan/todo/verify 中仅保留“从 X 到 Y”的 history 引用，**不**视为遗留）。

## 未重命名项（1.0.2 范围外）

- 仓库根目录路径 `ninerivers/ndp-tools/skills-manager-explorer/`：当前未重命名。重命名会改变 IDE workspace / git remote / 绝对路径引用，需另起决策与脚本。
- `.env.sample` 中 `SKILLS_MANAGER_DB` 环境变量：保持原样。该变量指向上游 Skills Manager SQLite 数据库（产品 Skills Manager），与本浏览器扩展产品名 `Skills Manager Explorer` 解耦。
- 1.0.2 plan / todo / verify 中的中文业务名文件前缀 `技能管家浏览器`：保留。按 `shared/references/ai-programming-docs-standard.md` 命名规范，文件名前缀使用中文业务名。
- OpenAPI `info.title` 与 `src/shared/contracts/openapi.ts:38` 中的中文 title、index.html `<title>`、server `console.info` 启动日志：仍为旧产品名，由 1.0.2 Phase 1 Task 2 替换；`bun run openapi:generate` 在 Task 2 完成后需再跑一次以刷新产物。

## 后续

1. Phase 1 Task 2：替换 `index.html`、OpenAPI title/description、server 启动日志中的旧产品名；重跑 `bun run openapi:generate`。
2. Phase 1 Task 3：README 重写。
3. Phase 2–5：vue-i18n 接入、文案抽取、区域感知、验证回归。
